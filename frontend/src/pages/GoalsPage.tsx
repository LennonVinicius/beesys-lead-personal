import {useState} from 'react'
import {useMutation,useQuery,useQueryClient} from '@tanstack/react-query'
import {api} from '@/services/api'
import {Card} from '@/components/ui/Card'
import {Button} from '@/components/ui/Button'

function bounds(period:string){const now=new Date();if(period==='WEEKLY'){const start=new Date(now);const day=(start.getDay()+6)%7;start.setDate(start.getDate()-day);start.setHours(0,0,0,0);const end=new Date(start);end.setDate(end.getDate()+7);return [start,end]}const start=new Date(now);start.setHours(0,0,0,0);const end=new Date(start);end.setDate(end.getDate()+1);return[start,end]}
export function GoalsPage(){const qc=useQueryClient();const q=useQuery({queryKey:['goals'],queryFn:()=>api.get<any[]>('/api/goals')});const[metric,setMetric]=useState('CONTACTS'),[target,setTarget]=useState(30),[period,setPeriod]=useState('DAILY'),[label,setLabel]=useState('');const create=useMutation({mutationFn:()=>{const[start,end]=bounds(period);return api.post('/api/goals',{metric,target,start_at:start.toISOString(),end_at:end.toISOString(),period,label:label||`${period==='WEEKLY'?'Meta semanal':'Meta de hoje'} - ${metric}`})},onSuccess:()=>{setLabel('');qc.invalidateQueries({queryKey:['goals']})}});return <div className="space-y-6"><div><h1 className="text-2xl font-bold">Metas</h1><p className="mt-1 text-sm text-muted">Defina metas diárias ou semanais de atividade, clientes e receita recorrente.</p></div><Card><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-[1fr_180px_180px_1fr_auto]"><label className="text-sm font-medium">Métrica<select className="mt-1 h-10 w-full rounded-lg border border-border px-3" value={metric} onChange={e=>setMetric(e.target.value)}><option value="LEADS">Leads encontrados</option><option value="CONTACTS">Contatos</option><option value="VISITS">Visitas</option><option value="INTERESTED">Interessados</option><option value="DEMOS">Demonstrações</option><option value="PROPOSALS">Propostas</option><option value="CLIENTS">Clientes</option><option value="MRR">MRR conquistado</option></select></label><label className="text-sm font-medium">Meta<input type="number" step={metric==='MRR'?'0.01':'1'} className="mt-1 h-10 w-full rounded-lg border border-border px-3" value={target} onChange={e=>setTarget(Number(e.target.value))}/></label><label className="text-sm font-medium">Período<select className="mt-1 h-10 w-full rounded-lg border border-border px-3" value={period} onChange={e=>setPeriod(e.target.value)}><option value="DAILY">Hoje</option><option value="WEEKLY">Semana</option></select></label><label className="text-sm font-medium">Nome da meta<input className="mt-1 h-10 w-full rounded-lg border border-border px-3" value={label} onChange={e=>setLabel(e.target.value)} placeholder="Ex.: 100 contatos na semana"/></label><Button className="self-end" onClick={()=>create.mutate()}>Criar meta</Button></div></Card><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{(q.data||[]).map(g=>{const value=Number(g.progress||0),goal=Number(g.target||0),mrr=g.metric==='MRR';return <Card key={g.id}><div className="flex items-start justify-between gap-2"><div><p className="text-sm text-muted">{g.label||g.metric}</p><p className="mt-2 text-2xl font-bold">{mrr?'R$ ':''}{value.toLocaleString('pt-BR',{maximumFractionDigits:2})} / {mrr?'R$ ':''}{goal.toLocaleString('pt-BR',{maximumFractionDigits:2})}</p></div><span className="rounded-full bg-primary-soft px-2 py-1 text-xs font-semibold text-primary">{g.period==='WEEKLY'?'Semana':'Hoje'}</span></div><div className="mt-3 h-2 overflow-hidden rounded-full bg-background"><div className="h-full bg-primary" style={{width:`${Math.min(100,100*value/Math.max(1,goal))}%`}}/></div></Card>})}</div></div>}
