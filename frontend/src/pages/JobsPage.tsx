import {useQuery} from '@tanstack/react-query';import {api} from '@/services/api';import type {SearchJob} from '@/types';import {Card} from '@/components/ui/Card';import {Badge} from '@/components/ui/Badge';import {Link} from 'react-router-dom';
export function JobsPage(){const q=useQuery({queryKey:['jobs'],queryFn:()=>api.get<SearchJob[]>('/api/searches?limit=50'),refetchInterval:5000});return <div className="space-y-6"><div><h1 className="text-2xl font-bold">Processamentos</h1><p className="mt-1 text-sm text-muted">Acompanhe as buscas e análises em andamento.</p></div><div className="space-y-3">{(q.data||[]).map(j=><Card key={j.id}><div className="flex flex-wrap items-center justify-between gap-3"><div><div className="flex items-center gap-2"><b>Busca #{j.id}</b><Badge tone={j.status==='DONE'?'success':j.status==='FAILED'?'danger':'primary'}>{j.status}</Badge></div><p className="mt-1 text-sm text-muted">{j.query_text} · {(j.radius_m/1000).toFixed(1)} km · {j.processed_items}/{j.total_items}</p></div>{j.status==='DONE'&&<Link className="text-sm font-semibold text-primary" to={`/leads?jobId=${j.id}`}>Abrir resultados</Link>}</div><div className="mt-3 h-1.5 overflow-hidden rounded-full bg-background"><div className="h-full bg-primary" style={{width:`${j.total_items?100*(j.processed_items+j.failed_items)/j.total_items:0}%`}}/></div></Card>)}</div></div>}
