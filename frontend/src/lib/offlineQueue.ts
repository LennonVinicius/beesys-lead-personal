type OfflineAction={id:string;method:'POST'|'PATCH';path:string;body:any;createdAt:string}
const DB='beesys-lead-offline-v1',STORE='actions'
const openDb=()=>new Promise<IDBDatabase>((resolve,reject)=>{const r=indexedDB.open(DB,1);r.onupgradeneeded=()=>{if(!r.result.objectStoreNames.contains(STORE))r.result.createObjectStore(STORE,{keyPath:'id'})};r.onsuccess=()=>resolve(r.result);r.onerror=()=>reject(r.error)})
export async function queueOfflineAction(method:'POST'|'PATCH',path:string,body:any){const db=await openDb();const action:OfflineAction={id:`${Date.now()}-${Math.random().toString(36).slice(2)}`,method,path,body,createdAt:new Date().toISOString()};await new Promise<void>((resolve,reject)=>{const tx=db.transaction(STORE,'readwrite');tx.objectStore(STORE).put(action);tx.oncomplete=()=>resolve();tx.onerror=()=>reject(tx.error)});return action}
export async function listOfflineActions(){const db=await openDb();return new Promise<OfflineAction[]>((resolve,reject)=>{const r=db.transaction(STORE,'readonly').objectStore(STORE).getAll();r.onsuccess=()=>resolve(r.result as OfflineAction[]);r.onerror=()=>reject(r.error)})}
export async function removeOfflineAction(id:string){const db=await openDb();return new Promise<void>((resolve,reject)=>{const tx=db.transaction(STORE,'readwrite');tx.objectStore(STORE).delete(id);tx.oncomplete=()=>resolve();tx.onerror=()=>reject(tx.error)})}
