import {useQuery} from '@tanstack/react-query';import {api} from '@/services/api';import {Card} from '@/components/ui/Card';import {Link} from 'react-router-dom';import {ArrowRight} from 'lucide-react'
export function DashboardPage(){const q=useQuery({queryKey:['dashboard'],queryFn:()=>api.get<Record<string,number>>('/api/dashboard')});const d=q.data||{};const cards=[['Leads',d.total||0],['Prioridade alta',d.hot||0],['Visitados',d.visited||0],['Interessados',d.interested||0],['Clientes',d.clients||0],['Sem site',d.no_website||0]];return <div className="space-y-6"><div><h1 className="text-2xl font-bold">Dashboard</h1><p className="mt-1 text-sm text-muted">Visão rápida da operação comercial.</p></div><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-3">{cards.map(([l,v])=><Card key={String(l)}><p className="text-sm text-muted">{l}</p><p className="mt-2 text-3xl font-bold">{v}</p></Card>)}</div><Card><div className="flex items-center justify-between"><div><h2 className="font-semibold">Próxima ação</h2><p className="mt-1 text-sm text-muted">Encontre uma área no mapa e gere uma nova lista priorizada.</p></div><Link to="/search" className="inline-flex items-center gap-2 text-sm font-semibold text-primary">Buscar leads <ArrowRight className="h-4 w-4"/></Link></div></Card></div>}
